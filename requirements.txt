# Runtime dependencies. Kept deliberately small — every package here is
# installed on each Render free-tier deploy, and build time is deploy latency.

fastapi==0.115.6
uvicorn[standard]==0.34.0      # [standard] pulls in the websockets implementation
pydantic==2.10.4
PyYAML==6.0.2                  # declarative agent definitions
httpx==0.28.1                  # web.fetch, and fastapi.testclient

# LLM providers. Both are optional at runtime: the engine router degrades to a
# local fallback when no key is configured.
groq==0.15.0
google-genai==0.3.0

# Postgres driver. Unused while DATABASE_URL is unset, but present so pointing
# at a Postgres instance is a one-env-var change with no code edit.
psycopg[binary]==3.2.3

# Local embeddings for memory search. ONNX-based -- no PyTorch. Optional at
# runtime: if the model cannot be fetched, memory search falls back to
# keyword-only rather than failing.
fastembed==0.8.0
